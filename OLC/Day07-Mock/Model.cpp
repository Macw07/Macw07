#include <iostream>
#include <algorithm>
using namespace std;

int main(){
    freopen("hello.in", "r", stdin);
    freopen("hello.out", "w", stdout);

    fclose(stdin);
    fclose(stdout);
    return 0;
}