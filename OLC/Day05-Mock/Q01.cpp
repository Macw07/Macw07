#include <iostream>
#include <algorithm>
using namespace std;

int main(){
    freopen("first.in", "r", stdin);
    freopen("first.out", "w", stdout);
    cout << "laheedon e la le li pho la~" << endl;
    fclose(stdin);
    fclose(stdout);
    return 0;
}